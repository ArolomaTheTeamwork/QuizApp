import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const QuestionList = () => {
  return (
    <View>
      <Text> all question List</Text>
    </View>
  );
};

export default QuestionList;

const styles = StyleSheet.create({});
